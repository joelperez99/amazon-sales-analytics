# ----------------------------------------------------------------------------
# Amazon Sales Analytics - dependencias
# Python 3.11+
# ----------------------------------------------------------------------------

# --- Interfaz ---------------------------------------------------------------
streamlit>=1.40,<2.0

# --- Procesamiento de datos -------------------------------------------------
pandas>=2.1,<3.0
numpy>=1.26,<3.0

# --- Visualización ----------------------------------------------------------
plotly>=5.20,<7.0

# --- Excel ------------------------------------------------------------------
openpyxl>=3.1
XlsxWriter>=3.1
xlrd>=2.0            # lectura de archivos .xls antiguos

# --- Base de datos ----------------------------------------------------------
SQLAlchemy>=2.0,<3.0
psycopg2-binary>=2.9  # driver PostgreSQL (producción)

# --- Configuración y validación --------------------------------------------
python-dotenv>=1.0
pydantic>=2.5,<3.0
email-validator>=2.1   # requerido por pydantic.EmailStr

# --- Seguridad --------------------------------------------------------------
bcrypt>=4.1

# --- Detección de encoding --------------------------------------------------
charset-normalizer>=3.3

# --- Almacenamiento opcional en la nube ------------------------------------
# Descomenta según el backend que utilices (STORAGE_BACKEND en .env)
# boto3>=1.34          # Amazon S3
# supabase>=2.4        # Supabase Storage

# --- Pruebas ----------------------------------------------------------------
pytest>=8.0
pytest-cov>=5.0
